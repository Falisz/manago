# Frontend
## Imports order
1. `react`, `react-dom`, `react-router-dom`
2. `react-contextify`
3. custom context hooks (`/src/contexts/`)
4. custom webhooks (`/src/hooks/`)
5. custom components (`/src/components/`)
6. CSS imports (`/src/styles/`)
7. other imports
## Styles order
1. Global styles
2. Custom components styles
3. Other styles, including inline element styles
## File structure
1. `/src/components/` - custom components
2. `/src/contexts/` - custom context hooks
3. `/src/hooks/` - custom webhooks
